# speckit-wrapper (Node.js)

## Install & run locally (Linux recommended)
1. Ensure Node 20+ installed.
2. Ensure Rust + cargo installed.
3. Ensure spec-kit-mcp is available (the Dockerfile does this for you).

Install deps:
```bash
npm install
# If the MCP client package is not on npm, install it from GitHub, for example:
# npm install github:modelcontextprotocol/typescript-sdk
